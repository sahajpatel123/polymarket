# Strategy profiles. `newsom-mm` is tuned for the Gavin Newsom 2028 Dem-nomination
# market from a live microstructure sample (2026-07-06): price ~0.195, tick 0.001,
# 2-tick spread, thin touch (~$20-250), VERY quiet tape (~0.1 trades/min),
# rewards: min 100 shares/order, 5.5c band, ~$52/day rate, 25% maker-rebate pool.

[profiles.newsom-mm]
# --- fair value ---
micro_levels        = 3
flow_ewma_halflife_s = 120
# --- spread / skew ---
gamma               = 0.6     # inventory skew; sized so full cap leans ~1-2 ticks
delta_min_ticks     = 2       # half-spread floor = 2 ticks (market spread is 2)
c_vol               = 1.5
c_tox               = 3.0
# --- vol horizons ---
vol_short_halflife_s = 10
vol_long_halflife_s  = 900
# --- sizing / inventory ($50 orders, $200 net max position) ---
base_size_usdc      = 100.0   # per token side, split into `layers` orders
q_max_usdc          = 200.0   # NET directional cap (YES minus NO exposure)
q_soft_frac         = 0.6     # stop adding at net $120, exits get urgent
layers              = 2       # 2 x $50 YES orders; NO auto-sizes to reward floor
reward_size_mult    = 1.5     # bump reward-eligible orders to 1.5x the min (100 -> 150 sh)
layer_step_ticks    = 2
# --- placement / churn ---
reprice_ticks       = 2       # ignore 1-tick FV flicker (hold queue position)
resize_frac         = 0.2
min_edge_ticks      = 1       # never bid above FV - 1 tick
# --- regime (damped for a quiet tape: one 250-share print must not flap us) ---
event_cooloff_s     = 30      # shorter: this is a deep, slow market
event_jump_ticks    = 6       # 0.006 = ~3% jump -> pull quotes
event_sweep_mult    = 6.0     # print must be >=6 order-sizes
event_sweep_frac    = 1.0     # AND consume ~all near-touch depth to count as a sweep
trend_flow_z        = 1.8
# --- lifecycle (resolves 2028 — irrelevant here) ---
reduce_only_hours   = 24
halt_before_hours   = 2
# --- exits ---
merge_min_size      = 20.0
use_intelligence    = true


[profiles.political-longdated]
# General-purpose long-dated political market profile: medium size, balanced
# skew, moderate event sensitivity. Suitable for markets >30d to expiry with
# 2-5 tick spreads and typical $50-500k liquidity. If unsure which profile to
# use, start here.
micro_levels        = 3
flow_ewma_halflife_s = 120
gamma               = 0.6
delta_min_ticks     = 1
c_vol               = 1.5
c_tox               = 3.0
vol_short_halflife_s = 10
vol_long_halflife_s  = 900
base_size_usdc      = 50.0
q_max_usdc          = 250.0
q_soft_frac         = 0.6
layers              = 2
layer_step_ticks    = 2
reward_size_mult    = 1.0
reprice_ticks       = 2
resize_frac         = 0.2
min_edge_ticks      = 1
event_cooloff_s     = 60
event_jump_ticks    = 6
event_sweep_mult    = 4.0
event_sweep_frac    = 0.8
trend_flow_z        = 1.8
trend_vol_ratio     = 3.0
reduce_only_hours   = 24
halt_before_hours   = 2
merge_min_size      = 20.0


[profiles.political-hot]
# Hot (short-dated, high-volume) political market profile: smaller size,
# tighter spread, more sensitive to flow/vol. For markets <30d to expiry
# with tight spreads and active tape.
micro_levels        = 3
flow_ewma_halflife_s = 90
gamma               = 0.8
delta_min_ticks     = 1
c_vol               = 2.0
c_tox               = 4.0
vol_short_halflife_s = 8
vol_long_halflife_s  = 600
base_size_usdc      = 30.0
q_max_usdc          = 150.0
q_soft_frac         = 0.5
layers              = 1
layer_step_ticks    = 3
reward_size_mult    = 1.0
reprice_ticks       = 1
resize_frac         = 0.15
min_edge_ticks      = 1
event_cooloff_s     = 90
event_jump_ticks    = 4
event_sweep_mult    = 3.0
event_sweep_frac    = 0.6
trend_flow_z        = 1.2
trend_vol_ratio     = 2.5
reduce_only_hours   = 48
halt_before_hours   = 6
merge_min_size      = 20.0


# romania-pm: "Next PM of Romania" — Alexandru Nazare leg. Live sample 2026-07-06:
# price ~0.487/0.488 (near 50/50 -> max reward score, low extremity), tick 0.001,
# 1-tick spread, ~$4k/day tape, rewards: min 50 shares/order, 4.5c band, ~$257/day
# rate, 25% rebate. endDate is stale (past) but still accepting -> handled as no-end.
[profiles.romania-pm]
micro_levels        = 3
flow_ewma_halflife_s = 120
# --- spread / skew ---
gamma               = 0.6
delta_min_ticks     = 1       # tight market (1-tick spread); quote close to score
c_vol               = 1.5
c_tox               = 3.0
# --- vol horizons ---
vol_short_halflife_s = 10
vol_long_halflife_s  = 900
# --- sizing / inventory (MINIMUM reward size — thin/gap-prone book, so a fill
#     must be small & disposable, not a directional bag. Reward < max, on purpose) ---
base_size_usdc      = 22.0    # ~50 sh at these prices = the reward MIN, nothing more
q_max_usdc          = 100.0   # ceiling (min-size orders make accumulation slow anyway)
q_soft_frac         = 0.6
layers              = 1
reward_size_mult    = 1.0     # floor each order to exactly the 50-share reward min
layer_step_ticks    = 2
# --- placement / churn (STICKY: thin/noisy tape must not flap us out of the
#     reward queue; we reprice only on real moves, not microprice jitter) ---
reprice_ticks       = 3       # ignore <=3-tick FV jitter (hold queue position)
resize_frac         = 0.6     # don't cancel/replace on a regime size-flip (154<->77)
min_edge_ticks      = 1
# --- regime: rest through noise (high trend threshold) but pull HARD on a real
#     sweep/jump, since the gapped book makes a toxic fill costly ---
event_cooloff_s     = 30
event_jump_ticks    = 5       # 0.005 jump from a thin book -> pull (gap protection)
event_sweep_mult    = 3.0     # thinner book: a smaller print is a real sweep here
event_sweep_frac    = 0.8     # consuming ~80% of near-touch depth counts
trend_flow_z        = 2.6     # noisy flow z on a $4k/day tape -> raise the bar
trend_vol_ratio     = 5.0     # ~1 trade/hr: microprice jitter must NOT halve size
                              # (real gaps still caught by event_jump_ticks)
# --- lifecycle (end date stale/None -> these are inert; acceptingOrders governs) ---
reduce_only_hours   = 24
halt_before_hours   = 2
# --- exits ---
merge_min_size      = 20.0
use_intelligence    = true


# aggressive_30: fixed $30 toy profile (legacy). Prefer live_scaled with
# risk.bankroll_usdc for production — sizes are auto-scaled there.
[profiles.aggressive_30]
micro_levels        = 2
flow_ewma_halflife_s = 60
gamma               = 0.4
delta_min_ticks     = 3
c_vol               = 2.0
c_tox               = 5.0
vol_short_halflife_s = 8
vol_long_halflife_s  = 600
base_size_usdc      = 3.0
q_max_usdc          = 30.0
q_soft_frac         = 0.5
layers              = 2
layer_step_ticks    = 1
reward_size_mult    = 2.5
reprice_ticks       = 1
resize_frac         = 0.15
min_edge_ticks      = 2
event_cooloff_s     = 30
event_jump_ticks    = 4
event_sweep_mult    = 3.0
event_sweep_frac    = 0.7
trend_flow_z        = 1.5
trend_vol_ratio     = 2.5
reduce_only_hours   = 48
halt_before_hours   = 2
merge_min_size      = 5.0
use_as_reservation_price = true
bankroll_usdc       = 30.0
max_open_orders_per_market = 2  # cap: 2/side × 2 sides = 4 orders max per market


# live_scaled: production advanced path for multi-category scaled live MM.
# Frozen pre-profit baseline for A/B (tight reprice + low trend_vol → false TRENDING churn).
# Used only for offline scoreboard; not a live default.
[profiles.baseline_naive]
micro_levels        = 3
flow_ewma_halflife_s = 120
gamma               = 0.5
delta_min_ticks     = 1
c_vol               = 1.0
c_tox               = 1.5
vol_short_halflife_s = 10
vol_long_halflife_s  = 900
base_size_usdc      = 50.0
q_max_usdc          = 250.0
q_soft_frac         = 0.6
layers              = 2
layer_step_ticks    = 2
reward_size_mult    = 1.0
reprice_ticks       = 1       # over-churn
resize_frac         = 0.1
min_edge_ticks      = 1
event_cooloff_s     = 60
event_jump_ticks    = 6
event_sweep_mult    = 4.0
event_sweep_frac    = 0.8
trend_flow_z        = 1.2
trend_vol_ratio     = 1.8     # false TRENDING on quiet books
reduce_only_hours   = 24
halt_before_hours   = 2
exit_urgency_s      = 900
merge_min_size      = 20.0
use_as_reservation_price = false
bankroll_usdc       = 0.0


# Profit production: longdated-like uptime + C-01 false-TRENDING damp + bankroll scale.
# Edge floors / join discipline live in strategy code (edge.py + quoting.py).
# Judgment: DecisionFramework gates dead/toxic tape and learns band position.
[profiles.live_scaled]
micro_levels        = 3
flow_ewma_halflife_s = 120
gamma               = 0.6
delta_min_ticks     = 1
c_vol               = 1.5
c_tox               = 3.5
vol_short_halflife_s = 10
vol_long_halflife_s  = 900
base_size_usdc      = 50.0
q_max_usdc          = 250.0
q_soft_frac         = 0.55
layers              = 2
layer_step_ticks    = 2
reward_size_mult    = 1.0
reprice_ticks       = 2
resize_frac         = 0.2
min_edge_ticks      = 1
event_cooloff_s     = 45
event_jump_ticks    = 6
event_sweep_mult    = 4.0
event_sweep_frac    = 0.8
trend_flow_z        = 1.8
trend_vol_ratio     = 5.0     # C-01: suppress vol-only false TRENDING
reduce_only_hours   = 24
halt_before_hours   = 2
exit_urgency_s      = 900
merge_min_size      = 20.0
use_as_reservation_price = false
bankroll_usdc       = 0.0
use_intelligence    = true
max_open_orders_per_market = 4


# ── Scalp-tiny: capital-constrained for $50 bankroll ──
# Same tight/fast philosophy as scalp-hot, but sized for the actual capital.
# Used in backtests where risk caps aren't enforced by the engine.
[profiles.scalp-tiny]
micro_levels         = 3
flow_ewma_halflife_s = 60
gamma                = 0.3
delta_min_ticks      = 1
c_vol                = 0.5
c_tox                = 2.0
vol_short_halflife_s = 5
vol_long_halflife_s  = 300
base_size_usdc       = 4.0     # ~$4 per order on $50 capital
q_max_usdc           = 12.0    # max $12 inventory per market
q_soft_frac          = 0.85
layers               = 1
layer_step_ticks     = 1
reward_size_mult     = 1.0
reprice_ticks        = 1
resize_frac          = 0.15
min_edge_ticks       = 0
join_best_bid        = true
event_cooloff_s      = 15
event_jump_ticks     = 4
event_sweep_mult     = 2.5
event_sweep_frac     = 0.6
trend_flow_z         = 1.2
trend_vol_ratio      = 3.0
reduce_only_hours    = 6
halt_before_hours    = 1
exit_urgency_s       = 300
merge_min_size       = 10.0
use_as_reservation_price = false
use_intelligence     = false
max_open_orders_per_market = 2


# Scalp-hot: tight, fast, single-layer, flow-gated. For live use where the
# RiskManager caps exposure to the bankroll. Base size is scaled up by the
# engine's scale_profile_sizes when bankroll_usdc > 0.
[profiles.scalp-hot]
micro_levels         = 3
flow_ewma_halflife_s = 60
gamma                = 0.3
delta_min_ticks      = 1
c_vol                = 0.5
c_tox                = 2.0
vol_short_halflife_s = 5
vol_long_halflife_s  = 300
base_size_usdc       = 18.0
q_max_usdc           = 80.0
q_soft_frac          = 0.85
layers               = 1
layer_step_ticks     = 1
reward_size_mult     = 1.0
reprice_ticks        = 1
resize_frac          = 0.15
min_edge_ticks       = 0
join_best_bid        = true
event_cooloff_s      = 15
event_jump_ticks     = 4
event_sweep_mult     = 2.5
event_sweep_frac     = 0.6
trend_flow_z         = 1.2
trend_vol_ratio      = 3.0
reduce_only_hours    = 6
halt_before_hours    = 1
exit_urgency_s       = 300
merge_min_size       = 10.0
use_as_reservation_price = false
use_intelligence     = false
max_open_orders_per_market = 2



# Scalp-safe: scalp-hot quoting + TP/SL risk management. Controlled risk per trade.
[profiles.scalp-safe]
micro_levels         = 3
flow_ewma_halflife_s = 60
gamma                = 0.3
delta_min_ticks      = 1
c_vol                = 0.5
c_tox                = 2.0
vol_short_halflife_s = 5
vol_long_halflife_s  = 300
base_size_usdc       = 4.0
q_max_usdc           = 12.0
q_soft_frac          = 0.85
layers               = 1
layer_step_ticks     = 1
reward_size_mult     = 1.0
reprice_ticks        = 1
resize_frac          = 0.15
min_edge_ticks       = 0
join_best_bid        = true
event_cooloff_s      = 15
event_jump_ticks     = 4
event_sweep_mult     = 2.5
event_sweep_frac     = 0.6
trend_flow_z         = 1.2
trend_vol_ratio      = 3.0
reduce_only_hours    = 6
halt_before_hours    = 1
exit_urgency_s       = 300
merge_min_size       = 10.0
use_as_reservation_price = false
use_intelligence     = false
max_open_orders_per_market = 2
take_profit_pct      = 0.02
stop_loss_pct        = 0.01
max_risk_per_trade_usdc = 2.0
tp_sl_urgency_s      = 60.0

# Balanced-MM: middle ground between scalp-hot (aggressive) and live_scaled
# (over-conservative). Joins the touch like scalp-hot so it GETS fills, but
# with moderate size, tighter toxicity control, and TP/SL protection so a
# bad fill can't bleed the account. The fill-model + governor overlay adds
# the selectivity layer on top of this quoting behaviour.
[profiles.balanced-mm]
micro_levels         = 3
flow_ewma_halflife_s = 90
gamma                = 0.5
delta_min_ticks      = 1
c_vol                = 1.0
c_tox                = 2.0
vol_short_halflife_s = 8
vol_long_halflife_s  = 600
base_size_usdc       = 15.0
q_max_usdc           = 60.0
q_soft_frac          = 0.7
layers               = 2
layer_step_ticks     = 1
reward_size_mult     = 1.0
reprice_ticks        = 1
resize_frac          = 0.15
min_edge_ticks       = 0
join_best_bid        = true
event_cooloff_s      = 20
event_jump_ticks     = 5
event_sweep_mult     = 3.0
event_sweep_frac     = 0.7
trend_flow_z         = 1.5
trend_vol_ratio      = 4.0
reduce_only_hours    = 12
halt_before_hours    = 2
exit_urgency_s       = 600
merge_min_size       = 15.0
use_as_reservation_price = false
use_intelligence     = true
take_profit_pct      = 0.02
stop_loss_pct        = 0.015
max_risk_per_trade_usdc = 3.0
