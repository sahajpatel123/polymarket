# 12h paper validation config — capital-adaptive, production live_scaled path.
[wallet]
chain_id = 137
signature_type = 3
clob_host = "https://clob.polymarket.com"
gamma_host = "https://gamma-api.polymarket.com"
data_api_host = "https://data-api.polymarket.com"
polygon_rpc = "https://polygon-bor-rpc.publicnode.com"

[engine]
debounce_ms = 250
quoter_tick_s = 60
reconcile_interval_s = 20
catalog_refresh_s = 300
heartbeat = true
heartbeat_interval_s = 5
journal = true
loop = "uvloop"
# Paper validation: keep discovery off so the experiment is on the fixed pair
auto_discovery_enabled = false
auto_discovery_hot_reload = false

[risk]
# Single capital knob — caps scale from bankroll
bankroll_usdc = 100.0
total_exposure_frac = 1.0
market_notional_frac = 0.40
event_group_frac = 0.50
daily_loss_frac = 0.10
market_loss_frac = 0.05
max_market_concentration_pct = 0.45
max_gas_cost_pct = 0.10
ws_stale_halt_s = 10.0
user_ws_blind_halt_s = 15.0
heartbeat_halt_failures = 3
max_order_error_rate = 0.25
# Absolutes filled by resolve_from_bankroll when bankroll > 0
max_total_exposure_usdc = 100.0
max_event_group_loss_usdc = 50.0
max_market_notional_usdc = 40.0
daily_loss_kill_usdc = 10.0
max_market_loss_usdc = 5.0

[execution]
rate_budget_fraction = 0.25
post_only = true
max_orders_per_batch = 15

[paths]
db = "livecfg/state.db"
journal_dir = "livecfg/journal"
log_dir = "livecfg/logs"
